upstream django {
	server django_gunicorn:8000;
}

server {
	listen 7777;

	location / {
		proxy_pass http://django;
	}

	location /static/ {
		alias /static/;
	}
	
	location /upload/ {
		alias /upload/;
	}
}
